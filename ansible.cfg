[defaults]
# Use the Beautiful Output callback plugin.
# stdout_callback = beautiful_output
# Use the stdout_callback when running ad-hoc commands.
# bin_ansible_callbacks = True
# display_skipped_hosts = False

# Role paths for organized role structure
roles_path = roles:roles/core:roles/shell:roles/dev:roles/devops:roles/cli:roles/apps:roles/media:roles/utils:roles/gaming

