# Git configuration for {{ item.gitdir }}
# Generated by Ansible

[user]
{% if item.name | default('') != '' %}
    name = {{ item.name }}
{% endif %}
{% if item.email | default('') != '' %}
    email = {{ item.email }}
{% endif %}
{% if item.signingkey | default('') != '' %}
    signingkey = {{ item.signingkey }}
{% endif %}

# Additional settings for this gitdir can be added here
