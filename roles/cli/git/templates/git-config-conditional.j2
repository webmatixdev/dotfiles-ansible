# Git configuration for {{ processed_user.gitdir }}
# Generated by Ansible

[user]
{% if processed_user.name | default('') != '' %}
    name = {{ processed_user.name }}
{% endif %}
{% if processed_user.email | default('') != '' %}
    email = {{ processed_user.email }}
{% endif %}
{% if processed_user.signingkey | default('') != '' %}
    signingkey = {{ processed_user.signingkey }}
{% endif %}

# Additional settings for this gitdir can be added here
